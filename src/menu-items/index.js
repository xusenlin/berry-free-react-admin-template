import pages from './pages';

import tasks from './tasks';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [pages, tasks]
};

export default menuItems;
